apiVersion: v1
kind: Service
metadata:
  name: microservicio-datum-4
  labels:
    app: microservicio-datum-4   
spec:
  ports:
  - name: http
    port: 8080
  selector:
    app: microservicio-datum-4
